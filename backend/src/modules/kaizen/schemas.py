from pydantic import BaseModel, field_validatorfrom typing import Optionalfrom datetime import datetimeclass KaizenCreate(BaseModel):    title: str    description: str    tipo: str = "processos"    priority: str = "media"    status: str = "Identificados"    departamento: Optional[str] = None    responsavel: Optional[str] = None        @field_validator('title')    def validate_title(cls, v):        if len(v) < 3:            raise ValueError('Title deve ter mínimo 3 caracteres')        return vclass KaizenUpdate(BaseModel):    title: Optional[str] = None    description: Optional[str] = None    tipo: Optional[str] = None    priority: Optional[str] = None    status: Optional[str] = None    departamento: Optional[str] = None    responsavel: Optional[str] = Noneclass KaizenResponse(KaizenCreate):    id: int    tenant_id: str    created_by: Optional[str]    created_at: datetime    updated_at: datetimeclass KaizenSummaryResponse(BaseModel):    total_kaizens: int    total_identificados: int    total_analisando: int    total_implementando: int    total_resolvidos: int    score_melhoria: float    ganho_financiero_total: float    melhor_departamento: str