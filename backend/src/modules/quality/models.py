from dataclasses import dataclassfrom datetime import datetimefrom typing import Optional, List, Dict@dataclassclass QualityRecord:    id: int    tenant_id: str    audit_type: str        # interna, externa, cliente, fornecedor    department: str    finding: str           # descrição da não conformidade    severity: str          # menor, maior, crítica    status: str            # aberto, em_analise, resolvido    capa_action: str       # ação corretiva/preventiva    responsible: str    due_date: datetime    client_id: Optional[int] = None  # cliente da consultoria    project_id: Optional[int] = None  # projeto relacionado    consultant_id: Optional[int] = None  # consultor responsável    evidence: Optional[str] = None  # link para evidencia(foto,doc)    resolution_note: Optional[str] = None  # Notas de resolução    resolve_at: Optional[datetime] = None  # data de resolução    created_at: datetime    updated_at: datetime    # ➕ NOVO: Campos extras    priority: Optional[str] = None  # baixa, media, alta    category: Optional[str] = None  # processo, documento, fisico, digital    root_cause: Optional[str] = None  # causa raiz    impact: Optional[str] = None  # impacto (baixo, medio, alto)    recurrence: bool = False  # recorrente?    related_nc: Optional[List[int]] = None  # NC relacionadas    followup_date: Optional[datetime] = None  # data de acompanhamento    verified_by: Optional[str] = None  # quem verificou    verified_at: Optional[datetime] = None  # quando verificado   附件: Optional[List[str]] = None  # arquivos anexados    tags: Optional[List[str]] = None  # tags    comments: Optional[str] = None  # comentários extras