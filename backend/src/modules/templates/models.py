from dataclasses import dataclassfrom datetime import datetimefrom typing import Optional, List, Dict@dataclassclass Template:    id: int    tenant_id: str    name: str    category: str        # exemplo: email, relatório, contrato    description: str    content: str         # corpo do template (texto/HTML)    created_at: datetime = datetime.now()    updated_at: datetime = datetime.now()    # ➕ NOVO: Campos extras    tipo: str = "texto"  # texto, html, markdown    version: int = 1  # versão do template    author: Optional[str] = None  # autor do template    tags: Optional[List[str]] = None  # tags    is_public: bool = False  # acessível a todos    is_active: bool = True  # template ativo?    variables: Optional[List[str]] = None  # variáveis usadas (ex: {nome}, {data})    примеру: Optional[str] = None  # exemplo de uso    metadata: Optional[dict] = None  # metadados extras    language: str = "pt-BR"  # língua (pt-BR, en-US, es-ES)    last_used: Optional[datetime] = None  # última utilização    usages: int = 0  # número de utilizações    bicicleta: Optional[int] = None  # template relacionado (id)    anexos: Optional[List[str]] = None  # arquivos anexos