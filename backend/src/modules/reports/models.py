from dataclasses import dataclassfrom datetime import datetimefrom typing import Optional, List, Dict@dataclassclass Report:    id: int    tenant_id: str    name: str    type: str  # financeiro, qualidade, workforce, etc.    description: str    content: dict  # JSON com dados do relatório    created_at: datetime = datetime.now()    updated_at: datetime = datetime.now()    # ➕ NOVO: Campos extras    formato: str = "pdf"  # pdf, excel, csv, json    gerado_por: Optional[str] = None  # quem gerou    status: str = "gerado"  # gerado, pendente, falhou    url_download: Optional[str] = None  # link para download    tamanho_bytes: Optional[int] = None  # tamanho do arquivo    views: int = 0  # número de visualizações    last_viewed: Optional[datetime] = None  # última visualização    projeto_id: Optional[int] = None  # projeto relacionado    cliente_id: Optional[int] = None  # cliente relacionado    parametros: Optional[dict] = None  # parametros usados    intervalo_inicio: Optional[datetime] = None  # intervalo inicial    intervalo_fim: Optional[datetime] = None  # intervalo final    agendado: bool = False  # relatório agendado?    agendamento_recorrencia: Optional[str] = None  # diaria, semanal, mensal    agendamento_horario: Optional[str] = None  # horario geracao    publico: bool = False  # acessavel a todos    tags: Optional[List[str]] = None  # tags